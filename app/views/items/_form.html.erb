<%= form_for(@item) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="col-xs-2 col-md-4" id="inputForm")>
    <div class="form-group" id=formProd>
      <%= f.label :title %><br>
      <%= f.text_field :title, class: 'form-control col-lg-1', placeholder: 'Required' %>
    </div>

    <div class="form-group">
      <%= f.label :crit %><br>
      <%= f.number_field :crit, class: 'form-control', placeholder: 'Required'  %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
  </div>

  
<% end %>
